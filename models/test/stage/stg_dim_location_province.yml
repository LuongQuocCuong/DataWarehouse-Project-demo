version: 2

models:                  
  - name: stg_dim_location_province        
    columns:
      - name: province_id
        tests:
          - unique
          - not_null
      - name: province_name
        tests:
          - unique
          - not_null
      - name: country_id
        tests:
          - not_null
              