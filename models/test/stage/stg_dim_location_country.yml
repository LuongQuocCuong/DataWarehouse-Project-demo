version: 2

models:                  
  - name: stg_dim_location_country        
    columns:
      - name: country_id
        tests:
          - unique
          - not_null
      - name: country_name
        tests:
          - not_null
            
              